/*switch (key) {
    case value:
        
        break;

    default:
        break;
}
The switch statement in JavaScript is a control flow statement that evaluates an expression and executes a block of code based on matching a case clause.
*/
switch (expression) {
  case value1:
    // Code to execute if expression === value1
    break;
  case value2:
    // Code to execute if expression === value2
    break;
  // ... more cases
  default:
  // Code to execute if no case matches
}

